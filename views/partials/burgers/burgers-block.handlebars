<li>

	{{burgername}}
	<button class="change-devoured" data-id="{{id}}"}>
		{{#unless devoured}}Ready to Eat!{{/unless}}{{#if devoured}}Delete Burger{{/if}}
	</button>

	{{!-- <button class="delete-burgers" data-id="{{this.id}}">DELETE!</button> --}}
</li>
